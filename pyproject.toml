[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "veillock"
version = "0.2.0"
description = "Consent-gated camera protection via AZ-OS: natural camera/video veil unless you turn it off or accept a call (VeilLock, Aziel Eliab)."
readme = "README.md"
license = {file = "LICENSE"}
authors = [{name = "Aziel Eliab"}]
requires-python = ">=3.10"
keywords = [
    "encryption",
    "streaming",
    "display",
    "forward-secrecy",
    "aes-gcm",
    "privacy",
    "consent",
    "az-os",
    "camera",
]
classifiers = [
    "Development Status :: 4 - Beta",
    "License :: OSI Approved :: Apache Software License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Security :: Cryptography",
    "Intended Audience :: Developers",
]
dependencies = [
    "numpy>=1.24",
    "cryptography>=42.0",
]

[project.optional-dependencies]
dev = ["pytest>=7.4"]
tether = ["opencv-python-headless>=4.8", "pyvirtualcam>=0.11"]

[project.scripts]
veillock = "veillock.cli:main"

[project.urls]
Homepage = "https://github.com/AzielEliab/veillock"
Download = "https://github.com/AzielEliab/veillock/releases"
Issues = "https://github.com/AzielEliab/veillock/issues"

[tool.setuptools.packages.find]
include = ["veillock*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-q"
